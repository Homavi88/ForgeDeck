# Agent notes — ForgeDeck

Realtime audio is **browser-only** (Web Audio). Python is projects, analysis, stems, AI, export.

**Documentation is mandatory with code.** Follow `.cursor/rules/docs-sync.mdc`: same change set as the feature, update `CHANGELOG.md` (Unreleased) and the matching file under `.cursor/docs/`. Start at [`.cursor/docs/README.md`](.cursor/docs/README.md).

Do not leave PulseForge in copy. Do not claim Rubber Band or GPU Demucs unless that path actually ran. Do not advertise `.cursor/docs/` on the public GitHub README.
