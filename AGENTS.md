# Agent notes — ForgeDeck

Realtime audio is **browser-only** (Web Audio). Python is projects, analysis, stems, AI, export.

**Documentation is mandatory with code.** Follow `.cursor/rules/docs-sync.mdc`: same change set as the feature, update `CHANGELOG.md` (Unreleased) and the matching file under `docs/`. Start at [`docs/README.md`](docs/README.md).

Do not leave PulseForge in copy. Do not claim Rubber Band or GPU Demucs unless that path actually ran. Do not dump the architecture table onto the public GitHub `README.md`.
